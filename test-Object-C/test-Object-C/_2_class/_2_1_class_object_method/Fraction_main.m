//
//  Fraction_main.m
//  test-Object-C
//
//  Created by hades on 2021/6/28.
//  Copyright © 2021 hades. All rights reserved.
//

#import "Fraction.h"

//program 部分：调用。
/*int main(int argc, const char* argv[]){
    @autoreleasepool {
        
        // alloc是allocate的缩写。
        // alloc：创建对象 —— 分配内存存储空间，获得该类的实例。
        Fraction *myFraction = [Fraction alloc];
        // init : 初始化该类的实例的变量。
        myFraction = [myFraction init];
        
        // 等价于
        // 创建对象，并初始化。
        //        myFraction = [[Fraction alloc]init];
        
        [myFraction setDenominator:10];
        [myFraction setNumerator:50];
        [myFraction print]; // 50, 10
        
        // 直接访问对象的变量
        NSLog(@"param is %i, %i", myFraction.denominator,myFraction.numerator);     // param is 10, 50
        // 通过对象的get方法来访问对象的变量
        NSLog(@"param is %i, %i", [myFraction denominator],[myFraction numerator]); // param is 10, 50
    }
    return 0;
}*/

